export type CreateBetterAliasFormData = {
  alias: string;
  value: string;
  label?: string;
  snippetOnlyMode: boolean;
};

export type CreateBetterSnippetFormData = {
  alias: string;
  body: string;
  label?: string;
};
